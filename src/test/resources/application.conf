# these are our own config values defined by the app
test-conversion {
  samples {
    directory {
      input = "src/test/resources/input/"
      control = "src/test/resources/control/"
    }
  }
  stylesheet {
    directory = "src/test/resources/xslt/"
    filename {
      prefix = ""
    }
  }
}